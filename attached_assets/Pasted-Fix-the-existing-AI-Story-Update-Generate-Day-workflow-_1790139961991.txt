Fix the existing AI Story Update / Generate Day workflow. DO NOT redesign the app, remove existing content, or rebuild any existing features.

CURRENT BUG:
Story Update and Generate Day successfully generate proposed content. The review screen works and I can edit or accept generated items. However, when I press Accept/Approve, the approved content does NOT appear inside the corresponding phone apps.

Fix the data/persistence flow between AI-generated content and the existing app databases/state.

EXPECTED BEHAVIOR:

Story Update
→ Generate Phone Activity / Generate Day
→ AI creates proposed content
→ Review
→ Edit if desired
→ Accept / Approve
→ approved content is permanently inserted into the correct app
→ the corresponding app immediately refreshes and displays the new content

Map generated content to the correct existing collections/data stores:

Diary entry → Diary
Quick Note → Quick Notes
Note → Notes
Message → Messages and the correct contact conversation
Instagram post → Instagram Feed/Profile
Instagram Story → Instagram Stories
Echo post → Echo Timeline/Profile
Echo draft → Echo Drafts
Gallery item → Gallery and the specified album
Browser search → Browser Search History
Browsing event → Browser History
Call → Phone Recents and relevant contact history
Missed call → Phone Recents + Notifications when appropriate
Voicemail → Phone Voicemail
Voice Memo → Voice Memos
Music activity → Music
Studio project/demo → Studio
Calendar event → Calendar
Place → Places
File → Files
Notification → Notifications

IMPORTANT:
Use the SAME persistent storage/data layer already used by manual Edit Mode.

AI-generated approved content and manually created content must use the same schema and collections.

Do not create a separate temporary AI-only data store.

When an item is approved:

1. Validate and normalize its data.
2. Insert it into the correct existing collection.
3. Generate a unique ID.
4. Preserve its generated date/time.
5. Mark its source as “ai-generated” internally if useful.
6. Persist it.
7. Update application state immediately.
8. Make it visible in the corresponding app without requiring a page reload.

APPROVE ALL:
Fix Approve All so every approved item is routed to its correct destination.

INDIVIDUAL APPROVE:
Fix individual Accept/Approve buttons as well.

EDIT THEN APPROVE:
If I edit generated content in the review screen, save the EDITED version rather than the original generated version.

NO DUPLICATES:
Prevent repeated button taps or page refreshes from inserting the same approved item multiple times.

PERSISTENCE TEST:
After approving generated content:

* navigate to the relevant app and verify it appears
* close and reopen the PWA and verify it remains
* refresh and verify it remains

DEBUGGING:
Inspect the existing manual Edit Mode implementation because manually created content already works. Reuse the same create/save functions for AI-approved content instead of implementing a second persistence mechanism.

Add clear error handling. If saving fails, show:
“Could not save generated activity”
and log the underlying error for debugging.

Do NOT report success unless the approved records were actually written to persistent storage.

TEST THE FIX using generated examples for:
1 Diary entry
1 Quick Note
1 Browser search
1 Echo post

Approve them and verify that all four appear in their corresponding apps and persist after refresh.

Preserve all existing user data and uploaded Gallery photos.